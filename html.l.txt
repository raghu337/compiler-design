%{
#include <stdio.h>
%}

%%
\<[^>]+\>    { printf("HTML Tag: %s\n", yytext); }
.|\n         ; 
%%

int yywrap() 
{
    return 1;
}

int main() 
{
    printf("Enter HTML content (Press Ctrl+D to stop):\n");
    yylex();
    return 0;
}